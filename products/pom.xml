<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>co.com.netec</groupId>
	<artifactId>products</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>Api Products</name>
	<description>Api products para la gestion de todos los productos del wokshop demostrativo al team bancolombia</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<organization>
        <name>developer.io</name>
        <url>https://developer.io/</url>
    </organization>

    <developers>
        <developer>
            <id>wortiz</id>
            <name>Wilman Ortiz</name>
            <email>wilman.ortiz@gmail.com</email>
            <url>https://developer.io/</url>
            <organization>developer.io</organization>
            <organizationUrl>https://developer.io/</organizationUrl>
            <roles>
                <role>Desarrollador Senior</role>
            </roles>
            <timezone>America/Bogota</timezone>
            <properties>
                <picture>https://www.todacolombia.com/imagenes/informacion-colombia/Bandera_de_Colombia_Flag_of_Colombia.png</picture>
            </properties>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/wortiz1027/workshop</url>
        <connection>scm:git@github.com:wortiz1027/workshop.git</connection>
        <developerConnection>scm:git:git@github.com:wortiz1027/workshop.git</developerConnection>
        <tag>main</tag>
    </scm>
	<properties>
		<java.version>21</java.version>
		<logstash-logback-encoder.version>8.0</logstash-logback-encoder.version>
		<spring-cloud.version>2025.1.3</spring-cloud.version>
	</properties>
	<profiles>
		<!-- 🚀 Perfil 1: Desarrollo Local (Activado por Defecto) -->
		<profile>
			<id>dev</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<spring.profiles.active>dev</spring.profiles.active>
			</properties>
		</profile>

		<!-- 🚀 Perfil 2: Producción / Docker Contenedores -->
		<profile>
			<id>prod</id>
			<properties>
				<spring.profiles.active>prod</spring.profiles.active>
				<logstash-logback-encoder.version>8.0</logstash-logback-encoder.version>
				<spring-cloud.version>2025.1.3</spring-cloud.version>
			</properties>
		</profile>
	</profiles>
	<!-- 🚀 AGREGADO: Bloque de control de versiones unificadas de OpenTelemetry -->
	<dependencyManagement>
		<dependencies>
			<!-- Controla el núcleo (API, SDK y Exportadores como OTLP) -->
			<dependency>
				<groupId>io.opentelemetry</groupId>
				<artifactId>opentelemetry-bom</artifactId>
				<version>1.43.0</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<!-- Controla las extensiones (Como las clases de Incubator que necesitas) -->
			<dependency>
				<groupId>io.opentelemetry.instrumentation</groupId>
				<artifactId>opentelemetry-instrumentation-bom</artifactId>
				<version>2.10.0</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>

			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<!-- Dependencias de Web -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-restclient</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webclient</artifactId>
		</dependency>

		<!-- Dependencias de Storage -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Dependencias de Observabilidad -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-aspectj</artifactId>
		</dependency>

		<!-- Dependencias de Observabilidad -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>		
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-opentelemetry</artifactId>
		</dependency>
		<dependency>
			<groupId>io.opentelemetry.instrumentation</groupId>
			<artifactId>opentelemetry-instrumentation-annotations</artifactId>
		</dependency>
		<dependency>
			<groupId>net.ttddyy.observation</groupId>
			<artifactId>datasource-micrometer-spring-boot</artifactId>
			<version>2.2.1</version>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>net.logstash.logback</groupId>
			<artifactId>logstash-logback-encoder</artifactId>
			<version>${logstash-logback-encoder.version}</version>
		</dependency>

		<!-- 🚀 REQUERIDO: Habilita el soporte prioritario para archivos bootstrap.yaml en Spring Boot 3.x -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-bootstrap</artifactId>
		</dependency>

		<!-- 🔌 Cliente de Configuración Centralizada -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-config-client</artifactId>
		</dependency>

		<!-- 📡 Cliente de Descubrimiento Eureka -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>

		<!-- Dependencias de Test -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-restclient-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>		
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!-- CONFIGURACIÓN DE JACOCO -->
        <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>0.8.11</version> <!-- Versión totalmente estable para Java 17/21 -->
            <executions>
                <!-- 1. Inicializa el agente de JaCoCo antes de que corran los tests -->
                <execution>
                    <id>prepare-agent</id>
                    <goals>
                        <goal>prepare-agent</goal>
                    </goals>
                </execution>
                
                <!-- 2. Genera el reporte HTML/XML después de ejecutar las pruebas -->
                <execution>
                    <id>report</id>
                    <phase>verify</phase>
                    <goals>
                        <goal>report</goal>
                    </goals>
                    <configuration>
                        <!-- Exclusiones críticas para el taller (No medimos cobertura en objetos sin lógica) -->
                        <excludes>
                            <exclude>co/com/netec/microservice/dto/**</exclude>
                            <exclude>co/com/netec/microservice/entity/**</exclude>
                            <exclude>co/com/netec/microservice/exception/**</exclude>
                            <exclude>co/com/netec/microservice/Application.class</exclude>
                        </excludes>
                    </configuration>
                </execution>

                <!-- 3. REGLA DE CONTROL: Rompe la compilación si no se cumple el mínimo de cobertura -->
                <execution>
                    <id>check-coverage</id>
                    <goals>
                        <goal>check</goal>
                    </goals>
                    <configuration>
                        <rules>
                            <rule>
                                <element>BUNDLE</element>
                                <limits>
                                    <limit>
                                        <counter>LINE</counter>
                                        <value>COVEREDRATIO</value>
                                        <minimum>0.55</minimum> <!-- Exige un 80% mínimo de cobertura para compilar -->
                                    </limit>
                                    <limit>
                                        <counter>BRANCH</counter>
                                        <value>COVEREDRATIO</value>
                                        <minimum>0.55</minimum> <!-- Exige un 70% mínimo en ramificaciones (if/else) -->
                                    </limit>
                                </limits>
                            </rule>
                        </rules>
                        <excludes>
                            <exclude>co/com/netec/microservice/dto/**</exclude>
                            <exclude>co/com/netec/microservice/entity/**</exclude>
                            <exclude>co/com/netec/microservice/exception/**</exclude>
                            <exclude>co/com/netec/microservice/Application.class</exclude>
                        </excludes>
                    </configuration>
                </execution>
            </executions>
        </plugin>
		</plugins>
	</build>

</project>
